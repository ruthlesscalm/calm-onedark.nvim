return {
  "mfussenegger/nvim-jdtls",
  ft = "java",
  -- Note: `jdtls` requires external Java language server installation and workspace setup.
  config = function()
    -- Keep minimal: project-specific jdtls setup is recommended (workspace, bundles)
  end,
}
